import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mehvozsoft.goodnews',
  appName: 'Good News',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
